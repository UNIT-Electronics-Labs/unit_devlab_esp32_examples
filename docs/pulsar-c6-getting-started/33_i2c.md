# I2C (Inter-Integrated Circuit)

Descubre el protocolo de comunicación I2C y aprende a comunicarte con dispositivos I2C usando la placa PULSAR C6. Esta sección cubre la configuración del bus I2C y la comunicación con periféricos I2C.

## Descripción general de I2C

I2C (Inter-Integrated Circuit) es un bus de comunicación serie síncrono, multi-maestro y multi-esclavo. Se usa comúnmente para conectar periféricos de baja velocidad con procesadores y microcontroladores. La placa PULSAR C6 incluye comunicación I2C para interactuar con distintos dispositivos.

## Detalles de pinout

La siguiente tabla muestra el pinout de las conexiones I2C en la PULSAR C6, detallando las asignaciones de SDA y SCL.

| Pin          | Función             |
|--------------|---------------------|
| A4 (SDA)/D18 | GPIO22 / SDIO_DATA2 |
| A5 (SCL)/D19 | GPIO23 / SDIO_DATA3 |

Pinout ESP32-C6

## I2C LP - Inter-Integrated Circuit de bajo consumo

El modo I2C LP (Low Power) está diseñado para reducir el consumo de energía durante la comunicación I2C. Es útil en dispositivos alimentados por batería. En este modo, el bus opera a frecuencias menores y usa técnicas para minimizar el consumo.

<div id="figure_i2c_lp">

<figure>
<img src="/sphinx-static/jst_lp.png" class="align-center"
style="width:20.0%" alt="/sphinx-static/jst_lp.png" />
<figcaption>Pines I2C</figcaption>
</figure>

</div>

| Pin       | Function       |
|-----------|----------------|
| D13 (SDA) | GPIO6 /LP_SDA  |
| D11 (SCL) | GPIO7 / LP_SCL |

Pinout ESP32-C6 LP

<figure>
<img src="/sphinx-static/c6_oled.png" class="align-center"
style="width:50.0%" alt="/sphinx-static/c6_oled.png" />
<figcaption>Pines I2C</figcaption>
</figure>

## Escaneo de dispositivos I2C

Para escanear dispositivos I2C conectados al bus, puedes usar el siguiente fragmento de código:

### MicroPython

``` python
import machine

i2c = machine.I2C(0, scl=machine.Pin(7), sda=machine.Pin(6))
devices = i2c.scan()

for device in devices:
    print("Device found at address: {}".format(hex(device)))
```

### C++

``` cpp
#include <Wire.h>

void setup() {
  // in setup
  Wire.setSDA(6);
  Wire.setSCL(7);
  Wire.begin();
  Serial.begin(115200); // Start serial communication at 115200 baud rate
  while (!Serial); // Wait for serial port to connect
  Serial.println("\nI2C Scanner");
}

void loop() {
  byte error, address;
  int nDevices;

  Serial.println("Scanning...");

  nDevices = 0;
  for(address = 1; address < 127; address++ ) {
    // The i2c_scanner uses the return value of the Write.endTransmisstion to see if
    // a device did acknowledge to the address.
    Wire.beginTransmission(address);
    error = Wire.endTransmission();

    if (error == 0) {
      Serial.print("I2C device found at address 0x");
      if (address<16) 
        Serial.print("0");
      Serial.print(address, HEX);
      Serial.println("  !");

      nDevices++;
    }
    else if (error==4) {
      Serial.print("Unknown error at address 0x");
      if (address<16)
        Serial.print("0");
      Serial.println(address, HEX);
    }    
  }
  if (nDevices == 0)
    Serial.println("No I2C devices found\n");
  else
    Serial.println("done\n");

  delay(5000);           // wait 5 seconds for next scan
}
```

## Pantalla SSD1306

<div id="figura-ssd1306-display">

<figure>
<img src="/sphinx-static/oled.jpg" class="align-center"
style="width:50.0%" alt="/sphinx-static/oled.jpg" />
<figcaption>Pantalla SSD1306</figcaption>
</figure>

</div>

La pantalla OLED monocromática de 128x64 pixeles con controlador SSD1306 se conecta mediante un conector JST de 1.25 mm y 4 pines. La siguiente tabla muestra el pinout de la conexión.

| Pin | Conexión |
|-----|------------|
| 1   | GND        |
| 2   | VCC        |
| 3   | SDA        |
| 4   | SCL        |

Pantalla SSD1306 Pinout

### Soporte de librerías

### MicroPython

La librería `ssd1306.py` para MicroPython en ESP32 y RP2040 es compatible con el controlador de pantalla SSD1306.

**Instalación**

1.  Abre [Thonny](https://thonny.org/).
2.  Copia la librería desde [ssd1306.py](https://github.com/UNIT-Electronics-MX/unit_devlab_micropython_libraries/blob/main/software/ssd1306/ssd1306.py).
3.  Pega la librería en un archivo nuevo en Thonny y guárdala como `ssd1306.py` en tu dispositivo.

**Configuración del microcontrolador**

``` python
SoftI2C(scl, sda, *, freq=400000, timeout=50000)
```

Cambia la siguiente línea según tu microcontrolador:

**Para ESP32**:

    >>> i2c = machine.SoftI2C(freq=400000, timeout=50000, sda=machine.Pin(21), scl=machine.Pin(22))

**Para RP2040**:

    >>> i2c = machine.SoftI2C(freq=400000, timeout=50000, sda=machine.Pin(4), scl=machine.Pin(5))

**Código de ejemplo**

``` python
import machine
from ssd1306 import SSD1306_I2C

I2C_SDA_PIN = 6
I2C_SCL_PIN = 7

i2c = machine.SoftI2C(sda=machine.Pin(I2C_SDA_PIN), scl=machine.Pin(I2C_SCL_PIN))

oled = SSD1306_I2C(128, 32, i2c)

oled.fill(1)
oled.show()

oled.fill(0)
oled.show()
oled.text('UNIT', 50, 10)
oled.text('ELECTRONICS', 25, 20)

oled.show()
```

Reemplaza `I2C_SDA_PIN` e `I2C_SCL_PIN` con los GPIO adecuados para tu configuración.

### C++

La librería `Adafruit_SSD1306` para Arduino es compatible con el controlador SSD1306.

**Instalación**

1.  Abre Arduino IDE.
2.  Ve a **Tools** -> **Manage Libraries**.
3.  Busca `Adafruit_SSD1306` y haz clic en **Install**.

**Código de ejemplo**

``` cpp
#include <Wire.h>
#include <Adafruit_GFX.h>
#include <Adafruit_SSD1306.h>

// OLED display TWI (I2C) interface
#define OLED_RESET     -1 // Reset pin # (or -1 if sharing Arduino reset pin)
#define SCREEN_WIDTH   128 // OLED display width, in pixels
#define SCREEN_HEIGHT  64  // OLED display height, in pixels
#define SDA_PIN        4   // SDA pin
#define SCL_PIN        5   // SCL pin

// Declare an instance of the class (specify width and height)
Adafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, OLED_RESET);

void setup() {
  Serial.begin(9600);

  // Initialize I2C
  Wire.setSDA(6);
  Wire.setSCL(7);
  Wire.begin();
  // Start the OLED display
  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) { // Address 0x3C for 128x64
    Serial.println(F("SSD1306 allocation failed"));
    for(;;); // Don't proceed, loop forever
  }

  // Clear the buffer
  display.clearDisplay();

  // Set text size and color
  display.setTextSize(1);
  display.setTextColor(SSD1306_WHITE);
  display.setCursor(0,0);
  display.println(F("UNIT ELECTRONICS!"));
  display.display();  // Show initial text
  delay(4000);        // Pause for 2 seconds
}

void loop() {
  // Increase a counter
  static int counter = 0;

  // Clear the display buffer
  display.clearDisplay();
  display.setCursor(0, 10); // Position cursor for new text
  display.setTextSize(2);   // Larger text size

  // Display the counter
  display.print(F("Count: "));
  display.println(counter);

  // Refresh the display to show the new count
  display.display();

  // Increment the counter
  counter++;

  // Wait for half a second
  delay(500);
}
```

### esp-idf

``` cpp
#include "ssd1306.h"
#include "driver/i2c.h"
#include "esp_log.h"

#define I2C_MASTER_NUM I2C_NUM_0
#define I2C_MASTER_SDA_IO 6
#define I2C_MASTER_SCL_IO 7
#define I2C_MASTER_FREQ_HZ 100000

static const char *TAG = "MAIN";

void scan_i2c_bus(void) {
   ESP_LOGI(TAG, "Scanning I2C bus...");
   for (uint8_t addr = 1; addr < 127; addr++) {
      i2c_cmd_handle_t cmd = i2c_cmd_link_create();
      i2c_master_start(cmd);
      i2c_master_write_byte(cmd, (addr << 1) | I2C_MASTER_WRITE, true);
      i2c_master_stop(cmd);
      esp_err_t ret = i2c_master_cmd_begin(I2C_MASTER_NUM, cmd, 100 / portTICK_PERIOD_MS);
      i2c_cmd_link_delete(cmd);
      if (ret == ESP_OK) {
        ESP_LOGI(TAG, "Found device at 0x%02X", addr);
      }
   }
   ESP_LOGI(TAG, "Scan complete.");
}

void app_main(void) {
   i2c_config_t conf = {
      .mode = I2C_MODE_MASTER,
      .sda_io_num = I2C_MASTER_SDA_IO,
      .scl_io_num = I2C_MASTER_SCL_IO,
      .sda_pullup_en = GPIO_PULLUP_ENABLE,
      .scl_pullup_en = GPIO_PULLUP_ENABLE,
      .master.clk_speed = I2C_MASTER_FREQ_HZ,
   };

   i2c_param_config(I2C_MASTER_NUM, &conf);
   i2c_driver_install(I2C_MASTER_NUM, conf.mode, 0, 0, 0);

   scan_i2c_bus(); // Optional

   ssd1306_init(I2C_MASTER_NUM);
   ssd1306_clear(I2C_MASTER_NUM);
   ssd1306_draw_text(I2C_MASTER_NUM, 0, "ESP32-C6 ");
   ssd1306_draw_text(I2C_MASTER_NUM, 2, "I2C Scan + OLED");
   ssd1306_draw_text(I2C_MASTER_NUM, 4, "Monosaurio");
}
```

## Ejemplo de aplicación

### Emotionally: rostro reactivo con MicroPython en Pulsar C6

Dale vida a tu placa con expresiones. Este proyecto muestra cómo usar la pantalla OLED SSD1306 y el sensor IMU MPU6050 con la PULSAR C6 para crear un rostro interactivo que reacciona al movimiento.

**Descripción del proyecto**

*Emotionally* es un proyecto expresivo que reacciona al movimiento: si lo agitas fuerte se enoja, si lo dejas quieto se duerme. Es un buen ejemplo de uso de sensores y pantallas para dar personalidad a sistemas embebidos.

**Componentes de hardware**

- Placa de desarrollo PULSAR C6
- Pantalla OLED SSD1306 (128x64)
- Sensor IMU MPU6050
- Conector I2C Qwiic

**Lógica de emociones**

- **Feliz**: movimiento suave
- **Sorprendido**: agitación moderada
- **Enojado**: movimiento brusco
- **Dormido**: quieto (con animación ZzZ)

**Configuración de software**

1.  Flashea MicroPython en la Pulsar C6
2.  Sube `main.py` y `mpu6050.py` usando mpremote o Thonny
3.  Enciende la placa: el rostro aparecerá en la pantalla
4.  Mueve la placa y observa cómo cambia la expresión en tiempo real.

Este proyecto es ideal para educación STEM, juguetes IoT, wearables y experimentos de interfaz de usuario.

<figure>
<img
src="https://raw.githubusercontent.com/UNIT-Electronics-MX/unit_emotionally_pulsar_c6_mp/refs/heads/main/hardware/resources/14.gif"
class="align-center" style="width:60.0%"
alt="https://raw.githubusercontent.com/UNIT-Electronics-MX/unit_emotionally_pulsar_c6_mp/refs/heads/main/hardware/resources/14.gif" />
<figcaption>Emotionally: demostración del rostro reactivo</figcaption>
</figure>

Para consultar el código completo, esquemáticos e instrucciones detalladas, visita la [página del proyecto en Hackster.io](https://www.hackster.io/mrcronos/emotionally-reacting-face-with-micropython-on-pulsar-c6-aa4a40).

